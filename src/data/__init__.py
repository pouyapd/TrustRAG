"""Research dataset construction for TrustRAG.

Turns third-party QA datasets into one schema the evaluation stack can consume,
without ever writing to the raw data. See `docs/DATASETS.md` for licensing.
"""
